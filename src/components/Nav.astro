<!-- ---
const links = [
    {
        label: "Sobre Nosotros",
        href: "/blog/aboutus",
    },
    {
        label: "GitHub",
        href: "github.com",
    },
    {
        label: "Contacto",
        href: "/contact",
    }
]
---

<nav class="w-full flex items-center h-20 absolute top-0 justify-between px-6 md:px-12 z-10">
    
    <section class="flex items-center gap-4 xl:gap-6">
        <img 
            class="aspect-square rounded-full object-cover object-center h-8 w-8 xl:w-12 xl:h-12" 
            src="/icon.webp" 
            alt="Icon Edutask"
        >
        <a href="/"><h1 class="text-xl font-bold">EduTask</h1></a>
    </section>
    
    <section class="space-x-12 hidden xl:block">
        {
            links.map(({href, label}) => (
                <a 
                class="text-lg font-light xl:hover:opacity-70 transition-opacity" 
                href={href}
                >{label}
                </a>
            ))
        }
    </section>

    <section class="xl:hidden">
        <button id="button-nav" class="transition-transform">
            <svg  
                xmlns="http://www.w3.org/2000/svg"  
                viewBox="0 0 24 24"  
                fill="none"  
                stroke="currentColor"  
                stroke-width="2"  
                stroke-linecap="round"  
                stroke-linejoin="round"  
                class="h-6 w-6">
            <path 
                stroke="none" 
                d="M0 0h24v24H0z" 
                fill="none"
            />
            <path d="M6 10l6 6l6 -6h-12"/>
        </svg>
        </button>
    </section>

</nav>
<section id="mobile-nav" class="h-screen w-screen justify-center items-center flex-col gap-6
backdrop-blur-xl absolute bottom-0 hidden xl:hidden delay-200"> 
        {
            links.map(({href, label}) => (
                <a 
                class="item-nav-mobile text-2xl font-semibold xl:hover:opacity-70 transition-opacity" 
                href={href}
                >{label}
                </a>
            ))
        }
</section>
<script>

    const initializeNavigation = () => {
        const $nav = document.getElementById("mobile-nav")
        const $button = document.getElementById("button-nav")
        const $items= document.getElementsByClassName("item-nav-mobile")

        $button?.addEventListener("click",() => {
            $button.classList.toggle("rotate-180")
            if($nav?.classList.contains("flex")){
                $nav?.classList.replace("flex", "hidden")
            }else{
                $nav?.classList.replace("hidden", "flex")
            }
        })
        // $items.map((item) => {
        //     item.addEventListener("click",()=>{
        //         $nav?.classList.replace("hidden", "flex") 
        //     })
        // })
    }

    window.addEventListener("DOMContentLoaded", ()=>{
        initializeNavigation()
        window.addEventListener("popstate", ()=> {
            initializeNavigation()
        })
    })
</script> -->