---
import Layout from "./Layout.astro";

interface Props {
    title : string
}

const { title} = Astro.props 
---

<Layout title={title}>
    <main class="mx-auto max-w-6xl mt-20 px-6 md:px-12 xl:px-0">
        <h2 class="text-3xl font-semibold">{title}</h2>
        <slot/>
    </main>
</Layout>